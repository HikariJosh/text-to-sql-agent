# MySQL
META_DB_PASSWORD=your_meta_db_password
DW_DB_PASSWORD=your_dw_db_password

# LLM
LLM_API_KEY=your_llm_api_key

# ---- 以下为可选覆盖项（有默认值，Docker 环境下自动覆盖） ----
# DB_META_HOST=localhost
# DB_DW_HOST=localhost
# QDRANT_HOST=localhost
# EMBEDDING_HOST=localhost
# ES_HOST=localhost
