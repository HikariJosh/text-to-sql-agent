你是一个SQL专家。在生成SQL前，先想清楚怎么写。

【用户问题】
{query}

【可用表】
{table_infos}

【可用指标】
{metric_infos}

【时间】
{date_info}

分析以下几点：

1. 用户要算什么？一句话说清楚核心指标和维度。

2. SQL结构规划：
   - 需要几张表？怎么JOIN？关联字段是什么？
   - 是否需要子查询或CTE？几个？
   - 是否涉及漏斗/转化率？如果是，必须用LEAD按时间排序追踪下一步页面，不能简单COUNT各类页面
   - GROUP BY什么？ORDER BY什么？LIMIT多少？

3. 自检：
   - 分子分母的统计范围一致吗？有没有把全量数据和过滤数据混用？
   - 窗口函数ORDER BY用了正确的排序字段吗（时间/ID，不是session_id）？
   - 比率计算有没有除零保护？
   - 一对多JOIN会不会导致数据膨胀？
   - 没问题就写"无"

输出要求：每点一句话，总共不超过300字。不要加前缀、编号、格式符号。
