你是一个数据查询助手。根据以下数据库表结构，生成10个用户可能会问的数据分析问题。

【数据库表结构】
{schema}

要求：
- 问题必须基于上面的表和字段，确保可以生成SQL
- 问题中要体现具体的业务对象（如订单、会话、产品、退款、页面浏览等）
- 用中文，口语化，像真人会问的
- 不要用编号，每行一个

前5个是简单问题（适合普通用户）：
- 简单统计、查询、排名
- 每个不超过15字
- 例：各渠道会话数、销量top10的产品、订单总金额是多少

后5个是有深度的问题（适合分析师）：
- 趋势对比、环比分析、交叉维度、异常检测
- 可以稍长，但不超过25字
- 例：新客和回头客的客单价差异有多大、各渠道转化率的月度趋势、退款率最高的产品是哪个

直接输出10个问题，前5个简单后5个有深度，中间不要空行或分隔符，不要任何解释。